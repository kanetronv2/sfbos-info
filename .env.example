# Provision Neon from the Vercel Marketplace, then make this variable
# available locally and in the Vercel project.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Used when generating absolute URLs in metadata and machine-readable output.
NEXT_PUBLIC_SITE_URL=https://sfbos.info
